import React from 'react'
import {Text,View,StyleSheet,FlatList} from 'react-native'
export const FocusHistory = ({history}) => {
  return(
    <View>
    <Text style={styles.focusHistory}></Text>
    </View>
  )
}
const styles=StyleSheet.create({
  focusHistory:{
    color:'white',
    
    
    
  }
})